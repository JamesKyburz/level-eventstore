{
  "name": "level-eventstore-ui",
  "version": "5.1.8",
  "scripts": {
    "prestart": "cd ./src/ui && yarn install && yarn build",
    "start": "node src/index",
    "missing": "dependency-sync --once"
  },
  "dependencies": {
    "level-eventstore": "^5.1.8",
    "moment": "2.18.1",
    "serve": "5.1.4",
    "server-base": "4.0.28"
  },
  "devDependencies": {
    "dependency-sync": "2.0.23"
  },
  "dependency-sync": {
    "args": [
      "src/index.js",
      "--debug"
    ],
    "yarn": {
      "args": [
        "--exact"
      ]
    }
  }
}
